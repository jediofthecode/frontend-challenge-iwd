// here we will use globally used app settings
// this allows for quick adjustment in various places in the app

export const API_URL = 'http://localhost:3000/api/'; // API Endpoints base url, must have successive /